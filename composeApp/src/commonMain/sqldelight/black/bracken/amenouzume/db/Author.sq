CREATE TABLE Author (
    author_id INTEGER NOT NULL PRIMARY KEY
);

selectById:
SELECT * FROM Author WHERE author_id = ?;

insert:
INSERT INTO Author(author_id) VALUES (NULL);

deleteById:
DELETE FROM Author WHERE author_id = ?;
